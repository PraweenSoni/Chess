<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Login - Chess Lobby</title>
  <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
  <style>
    body {
      background-image: url('assets/images/chess3.jpg');
      background-size: cover;
      background-position: center;
    }
  </style>
</head>

<body class="text-white flex items-center justify-center h-screen relative">
  <div class="absolute inset-0 bg-black/50"></div>
  <div class="bg-zinc-700 p-8 rounded-lg shadow-lg w-full max-w-md z-10">
    <h2 class="text-2xl font-bold mb-6 text-center">Welcome Back</h2>
    <!-- Error message display div -->
    <% if (typeof error !=='undefined' ) { %>
      <div class="bg-red-600 text-white p-2 rounded mb-4 text-center">
        <%= error %>
      </div>
      <% } %>

        <form action="/login" method="POST" class="space-y-4">
          <input type="text" name="email" placeholder="Email"
            class="w-full p-3 rounded bg-zinc-700 border border-zinc-600" required />
          <input type="password" name="password" placeholder="Password"
            class="w-full p-3 rounded bg-zinc-700 border border-zinc-600" required />

          <div class="flex items-center justify-between">
            <label class="flex items-center text-sm text-gray-400">
              <input type="checkbox" name="rememberMe" class="mr-2" />
              Remember Me
            </label>
            <a href="#" class="text-sm text-blue-400 hover:underline">Forgot Password?</a>
          </div>

          <button type="submit" class="w-full bg-green-600 hover:bg-green-700 p-3 rounded font-semibold">Login</button>
        </form>

        <div class="my-4 text-center text-sm text-gray-500">Or continue with</div>
        <div class="flex gap-4">
          <a href="/auth/google" class="flex-1 bg-red-500 hover:bg-red-600 p-2 rounded text-center">Google</a>
          <a href="/auth/github" class="flex-1 bg-gray-700 hover:bg-gray-800 p-2 rounded text-center">GitHub</a>
        </div>

        <p class="mt-6 text-sm text-gray-400 text-center">
          Don't have an account? <a href="/signup" class="text-green-400 hover:underline">Sign up</a>
        </p>
  </div>
</body>

</html>